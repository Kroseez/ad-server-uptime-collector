# 🕒 AD Server Uptime Collector

PowerShell-скрипт для централизованного сбора **uptime-статистики Windows‑серверов** через Active Directory.

Проект решает простую, но вечно актуальную задачу: понять, **какие сервера живы, какие нет и сколько они уже не перезагружались**.

Без агентов, без стороннего софта, только AD + PowerShell + RSAT.

---

## 🎯 Назначение

Скрипт автоматически:

* получает список серверов из заданной OU в Active Directory
* проверяет доступность каждого сервера по сети
* собирает uptime для доступных серверов
* формирует CSV‑отчёт с результатами

Результат подходит для:

* аудита
* планирования перезагрузок
* диагностики «сервер не трогали 800 дней»

---

## 🧩 Основная логика работы

1. Проверяется наличие директории `C:\Logs\Uptime`

   * если её нет, она создаётся автоматически
2. Проверяется наличие модуля `ActiveDirectory`
3. Из AD извлекаются все компьютеры из указанной OU (включая вложенные)
4. Для каждого сервера:

   * выполняется ping‑проверка
   * при доступности считывается `LastBootUpTime` через WMI
   * рассчитывается uptime
5. Формируется CSV‑отчёт со статусом каждого сервера

---

## 📋 Требования

### 🔧 Система

* Windows
* PowerShell 5.1+

### 📦 Компоненты

* RSAT (Remote Server Administration Tools)
* Модуль `ActiveDirectory`

### 🔐 Права

* Учетная запись с правами чтения AD
* Доступ к WMI на серверах

---

## ▶️ Использование

### Запуск с параметрами по умолчанию

```powershell
.\Get-ServerUptime.ps1
```

### Кастомный путь и имя отчёта

```powershell
.\Get-ServerUptime.ps1 -OutputDirectory "D:\Reports" -OutputFileName "Servers_Uptime.csv"
```

---

## 📄 Формат отчёта (CSV)

| Поле         | Описание                        |
| ------------ | ------------------------------- |
| ServerName   | DNS‑имя сервера                 |
| Status       | Online / Offline / WMI Error    |
| LastBootTime | Время последней загрузки        |
| Uptime       | Аптайм в днях / часах / минутах |
| CheckDate    | Дата и время проверки           |

Разделитель: `;`

---

## 🗂 Структура репозитория

```
ad-server-uptime-collector/
├── Get-ServerUptime.ps1
├── README.md
└── LICENSE
```

---

## ⚠️ ВАЖНО

* Скрипт **не вносит изменений** в AD или на серверах
* Используется WMI, убедитесь, что:

  * firewall разрешает подключения
  * WinRM/WMI не заблокированы
* Offline‑серверы всё равно попадут в отчёт

---

## 🌍 English Summary

PowerShell script that collects uptime statistics from all Windows servers located in a specified Active Directory OU.

Features:

* AD‑based server discovery
* Online/offline detection
* Uptime calculation via WMI
* CSV reporting
* RSAT‑compatible, agentless

---

## 📄 License

MIT License. Use it, modify it, automate it. Just don’t blame the script for ancient uptimes.

---

## 👤 Author

**Kroseez**
System Administrator / Enthusiast
